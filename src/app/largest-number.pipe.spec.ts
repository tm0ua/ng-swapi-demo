import { LargestNumberPipe } from './largest-number.pipe';

describe('LargestNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new LargestNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
